import express from 'express';
const router = express.Router();
import { auth } from '../../middlewares/auth';

/* User Routes */


/* Account and Shipping Method Routes */
//router.post("/account/edit", auth("arw"), editAccountMethod);


export default router;